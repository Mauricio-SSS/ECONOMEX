{
  "name": "centro-economex",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "echo ECONOMEX_STATIC_BUILD_ACTIVE && rm -rf dist .astro .wrangler node_modules/.vite && astro build",
    "preview": "npm run build && astro preview",
    "astro": "astro",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/mdx": "^5.0.4",
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.1.10"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^5.7.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
