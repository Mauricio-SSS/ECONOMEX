---
import SectionPlaceholder from '../components/placeholder/SectionPlaceholder.astro';
const query = Astro.url.searchParams.get('q') ?? '';
---

<SectionPlaceholder
  title="Buscar"
  description="Encuentra artículos, datos, temas y recursos publicados por Centro Economex."
  eyebrow="Búsqueda"
  status="Búsqueda en desarrollo"
  features={['Buscador por palabra clave', 'Filtros por tema y tipo de publicación', 'Resultados rápidos']}
  nextSteps={['Conectar índice de búsqueda local.', 'Agregar resultados destacados.', 'Incluir filtros por autor, tema y tipo de publicación.']}
>
  <form class="inline-form" action="/buscar" method="get" role="search">
    <label for="search-q" class="visually-hidden">Buscar</label>
    <input id="search-q" name="q" type="search" value={query} placeholder="Buscar artículos, datos o temas" />
    <button type="submit">Buscar</button>
    {query && <p class="form-note">La búsqueda avanzada se conectará pronto. Consulta mientras tanto la sección de artículos.</p>}
  </form>
</SectionPlaceholder>

<style>
  .inline-form { display: grid; gap: var(--space-3); margin-top: var(--space-6); background: #fff; border: 1px solid var(--color-border); border-radius: var(--radius-xl); padding: var(--space-5); box-shadow: var(--shadow-sm); }
  .inline-form input { min-height: 46px; border: 1px solid var(--color-border); border-radius: var(--radius-md); padding: 0.75rem 1rem; background: var(--color-bg-warm); }
  .inline-form button { min-height: 46px; border-radius: var(--radius-full); padding: 0.75rem 1.1rem; font-weight: 700; background: var(--color-primary); color: #fff; }
  .inline-form button:hover { background: var(--color-primary-hover); }
  .form-note { margin: 0; color: var(--color-text-muted); font-size: var(--fs-sm); }
  @media (min-width: 700px) { .inline-form { grid-template-columns: 1fr auto; align-items: center; } .inline-form .form-note { grid-column: 1 / -1; } }
</style>
